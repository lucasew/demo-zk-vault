{
    "scripts": {
        "build": "curl -L https://github.com/gohugoio/hugo/releases/download/v0.82.0/hugo_0.82.0_Linux-64bit.tar.gz -o hugo.tar.gz && tar -xf hugo.tar.gz && chmod +x ./hugo && ./hugo"
    }
}
